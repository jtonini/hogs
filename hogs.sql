CREATE TABLE hogs (
        t datetime default current_timestamp,
        host varchar(20),
        cpu_used varchar(20),
        cpu_total varchar(20),
        mem_used varchar(20),
        mem_total varchar(20)
        );
